# Server Configuration
server.port=9090
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/careersync_db
spring.datasource.username=root
spring.datasource.password=Achyut06
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JWT Configuration
app.jwt.secret=${JWT_SECRET:careersync-super-secret-key-change-this-in-production-2024}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${UPLOAD_DIR:uploads}

# Gemini AI Configuration
app.gemini.api.key=AIzaSyBpr5NDrhzhh42IUx7nVLLJhQzQ5nLoJLE
app.gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# Logging
logging.level.com.careersync=DEBUG
logging.level.org.springframework.security=WARN

# Swagger / OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# CORS - update with your frontend URL in production
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
